server:
  host: "10.88.0.1"
  port: 14348
uploadDir: "./uploads"
database:
  path: "data.db"
oauth:
  baseUrl: "https://..."
  endpoint: "https://..."
  clientID: "..."
  clientSecret: "..."
  hash: [...]
security:
  csrfAuthKey: [...]
sessionKey: [...]
